FROM python:3.13-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY app ./app

RUN useradd --create-home --uid 10001 loreholm
RUN chmod -R a+rX /app
USER loreholm

EXPOSE 8080
CMD ["uvicorn", "app.v2.app:app", "--host", "0.0.0.0", "--port", "8080"]
