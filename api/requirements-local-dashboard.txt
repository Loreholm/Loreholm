fastapi[standard]>=0.136.1
httpx
docker
# Embedding generation runs on the dashboard. Torch is installed separately
# from the PyTorch CPU index in Dockerfile.local-dashboard — including it
# here would pull the default PyPI build, which on arm64 drags in CUDA/cuDNN
# wheels (~1 GB) intended for Jetson/GH200. BYODB runs on commodity CPUs.
# `sentence-transformers` powers the MiniLM fallback path; `transformers` is
# what we use directly for Harrier-270M (decoder-only, not a native
# sentence-transformers target).
transformers>=4.40
sentence-transformers>=2.7

# Security constraints for transitive dependencies whose parents allow
# vulnerable versions.
cryptography>=46.0.7
idna>=3.15
python-dotenv>=1.2.2
python-multipart>=0.0.27
starlette>=0.49.1,<0.50
urllib3>=2.7.0
